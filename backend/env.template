# ================================
# AI Builder Space 配置
# ================================
# ChatGPT-5 API Token（通过AI Builder Space中转）
AI_BUILDER_TOKEN=your_ai_builder_token_here

# ================================
# 邮箱服务配置（价格提醒）
# ================================
# 如果使用 Gmail，需要生成应用专用密码
# 步骤：Google账号 → 安全性 → 两步验证 → 应用专用密码
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_gmail_app_password
SMTP_FROM_EMAIL=your_email@gmail.com
SMTP_FROM_NAME=Trade View 交易提醒

# ================================
# JWT 认证密钥（重要！）
# ================================
# 用于生成和验证JWT token，生产环境必须设置为强密码
# 可以使用以下命令生成：python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-secret-key-change-in-production

# ================================
# 数据库配置
# ================================
# 选项1：PostgreSQL（推荐，生产环境）
# 使用Supabase、Railway、Neon等免费PostgreSQL服务
# 格式：postgresql://user:password@host:port/database
# 示例：postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
DATABASE_URL=

# 选项2：SQLite（本地开发，不推荐生产环境）
# 如果DATABASE_URL未设置，将使用SQLite
# 数据库存储目录（Docker中使用/data实现持久化）
DB_DIR=/data

# ================================
# SSL配置（可选）
# ================================
# 如果遇到SSL证书验证错误（特别是macOS），可以临时禁用SSL验证
# 注意：仅用于开发环境，生产环境应使用有效证书
# DISABLE_SSL_VERIFY=true

# ================================
# 日志级别（可选）
# ================================
# LOG_LEVEL=INFO

# ================================
# 使用说明
# ================================
# 1. 复制此文件为 .env
#    cp env.template .env
#
# 2. 编辑 .env 文件，填入真实的配置值
#
# 3. 如果遇到SSL证书错误，添加: DISABLE_SSL_VERIFY=true
#
# 4. 重启后端服务
#    python3 main.py
