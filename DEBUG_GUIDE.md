# 🐛 可视化日志调试指南

## 📋 当前状态

✅ **后端服务已启动**
- 地址: http://localhost:3000
- API文档: http://localhost:3000/docs
- 日志文件: `backend/backend.log`

## 🔍 如何查看日志

### 1. 后端日志（终端实时查看）

```bash
# 实时查看后端日志
tail -f backend/backend.log

# 或者直接查看最后50行
tail -50 backend/backend.log
```

### 2. 后端日志（浏览器控制台）

访问 http://localhost:3000/docs 查看API文档和测试接口

### 3. 前端日志（浏览器开发者工具）

1. 打开浏览器（Chrome/Firefox/Edge）
2. 按 `F12` 或 `Cmd+Option+I` (Mac) 打开开发者工具
3. 切换到 **Console** 标签页
4. 所有前端日志都会显示在这里，带有emoji图标：
   - 🔐 认证相关
   - 💰 资金相关
   - 📡 API请求
   - ✅ 成功操作
   - ❌ 错误信息

## 📊 日志格式说明

### 后端日志格式
```
[时间戳] - [模块] - [级别] - [消息]
```

示例：
```
2026-01-11 22:14:26 - main - INFO - 🚀 启动 A股交易管理系统后端服务
2026-01-11 22:14:26 - main - INFO - ✅ 数据库初始化完成
2026-01-11 22:14:26 - auth - INFO - 🔐 [注册] 用户名: testuser, 邮箱: test@example.com
```

### 前端日志格式
```
[时间] [emoji] [级别] [消息]
数据: {...}
```

示例：
```
[22:14:26] 🔐 [INFO] [Auth] 初始化认证上下文
[22:14:26] ✅ [INFO] [Auth] 登录成功
数据: { userId: 1, username: "testuser" }
```

## 🧪 调试步骤

### 步骤1: 测试后端API

```bash
# 测试根路径
curl http://localhost:3000/

# 测试注册接口
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@example.com","password":"test123"}'

# 测试登录接口
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"test123"}'
```

### 步骤2: 启动前端

```bash
cd frontend
npm install  # 如果还没安装依赖
npm run dev
```

前端将在 http://localhost:5173 启动

### 步骤3: 在浏览器中测试

1. 打开 http://localhost:5173
2. 打开浏览器开发者工具 (F12)
3. 切换到 Console 标签
4. 尝试注册/登录，观察日志输出

## 📚 Swagger UI 使用

FastAPI 自动生成了交互式API文档，详细使用说明请查看：
- **Swagger UI**: http://localhost:3000/docs
- **使用指南**: 查看 `SWAGGER_UI_GUIDE.md` 文件

### 快速开始 Swagger UI

1. 打开 http://localhost:3000/docs
2. 注册用户获取Token: `POST /api/auth/register`
3. 点击右上角 **Authorize** 按钮
4. 输入: `Bearer <你的token>` (注意Bearer后有空格)
5. 现在可以测试所有API了！

## 📝 日志级别

### 后端日志级别
- **INFO**: 一般信息（启动、请求、成功操作）
- **WARN**: 警告（用户已存在、登录失败等）
- **ERROR**: 错误（异常、失败操作）

### 前端日志级别
- **DEBUG**: 调试信息（详细的数据流）
- **INFO**: 一般信息（操作成功、状态更新）
- **WARN**: 警告（API返回警告）
- **ERROR**: 错误（API失败、异常）

## 🎯 常见调试场景

### 场景1: 用户注册失败
1. 查看后端日志: `tail -f backend/backend.log`
2. 查看前端Console日志
3. 检查错误信息中的详细原因

### 场景2: API请求失败
1. 查看浏览器Network标签，检查请求和响应
2. 查看Console中的错误日志
3. 查看后端日志中的对应请求记录

### 场景3: 数据不显示
1. 检查前端Console是否有API调用日志
2. 检查Network标签中的API响应
3. 检查后端日志中的数据库查询

## 🔧 实时监控命令

```bash
# 在一个终端窗口监控后端日志
tail -f backend/backend.log | grep -E "(INFO|ERROR|WARN)"

# 在另一个终端窗口启动前端
cd frontend && npm run dev
```

## 📱 测试流程建议

1. **注册新用户** → 观察前后端日志
2. **登录** → 观察token生成和存储
3. **查看用户面板** → 观察资金数据获取
4. **添加交易记录** → 观察数据库操作
5. **查看持仓** → 观察数据查询
6. **AI分析** → 观察数据分析过程

## 🐛 问题排查

如果遇到问题：

1. **检查后端是否运行**
   ```bash
   curl http://localhost:3000/
   ```

2. **检查端口占用**
   ```bash
   lsof -i :3000  # 后端
   lsof -i :5173  # 前端
   ```

3. **查看完整错误日志**
   ```bash
   cat backend/backend.log | tail -100
   ```

4. **重启服务**
   ```bash
   # 停止后端
   pkill -f "python.*main.py"
   
   # 重新启动
   cd backend && python3 main.py > backend.log 2>&1 &
   ```
