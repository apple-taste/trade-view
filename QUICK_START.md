# ⚡ 快速启动指南

## ✅ 问题已修复

- ✅ bcrypt兼容性问题已修复（改用pbkdf2_sha256）
- ✅ 后端已启动并运行在 http://localhost:3000

## 🚀 启动步骤

### 1. 启动后端（已在运行）

后端已经在运行，如果需要重启：

```bash
cd /Users/ierx/cursor_workspace/trade-view/backend
python3 main.py
```

或者后台运行：
```bash
cd /Users/ierx/cursor_workspace/trade-view/backend
python3 main.py > backend.log 2>&1 &
```

### 2. 启动前端

**打开新终端窗口：**

```bash
cd /Users/ierx/cursor_workspace/trade-view/frontend
npm install  # 如果还没安装依赖
npm run dev
```

前端将在 http://localhost:5173 启动

### 3. 监控日志

**后端日志监控（新终端）：**

```bash
cd /Users/ierx/cursor_workspace/trade-view/backend
tail -f backend.log
```

**前端日志：**
- 打开浏览器 http://localhost:5173
- 按 F12 打开开发者工具
- 切换到 Console 标签
- 所有前端日志都会显示在这里

## 📊 实时监控命令

### 同时监控前后端日志

**终端1 - 后端日志：**
```bash
cd /Users/ierx/cursor_workspace/trade-view/backend
tail -f backend.log | grep -v "sqlalchemy.engine"  # 过滤SQL日志
```

**终端2 - 前端：**
```bash
cd /Users/ierx/cursor_workspace/trade-view/frontend
npm run dev
```

**终端3 - 浏览器：**
- 打开 http://localhost:5173
- 打开开发者工具 (F12)
- 查看Console日志

## 🔍 日志过滤技巧

### 只看重要日志

```bash
# 只看错误和重要信息
tail -f backend/backend.log | grep -E "(ERROR|INFO|✅|❌|🔐|💰)"

# 只看API请求
tail -f backend/backend.log | grep -E "(📥|📤)"

# 只看认证相关
tail -f backend/backend.log | grep -E "(注册|登录|auth)"
```

## ✅ 验证服务

```bash
# 检查后端
curl http://localhost:3000/
# 应该返回: {"message":"A股交易管理系统 API","docs":"/docs"}

# 检查Swagger UI
open http://localhost:3000/docs
```

## 🎯 现在开始测试

1. ✅ 后端已启动（http://localhost:3000）
2. ⏳ 启动前端（运行上面的命令）
3. ⏳ 打开浏览器访问 http://localhost:5173
4. ⏳ 打开开发者工具查看日志
5. ⏳ 开始测试功能！

## 📝 测试流程

1. **注册用户** → 观察前后端日志
2. **登录** → 观察token生成
3. **查看用户面板** → 观察API调用
4. **添加交易记录** → 观察数据库操作
5. **查看持仓** → 观察数据查询

## 🐛 如果遇到问题

- **后端未启动**: 检查端口3000是否被占用
- **前端无法连接**: 检查后端是否运行
- **注册失败**: 查看后端日志中的错误信息

现在可以开始测试了！🚀
