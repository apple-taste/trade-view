# 🍎 Mac 用户完整使用指南

## 📱 一、音效系统

### 1. 激活音频（必须）
**位置**：闹铃面板（黄色边框）右上角

**操作**：
```
点击 "🎵 激活音频" 按钮（紫色）
  ↓
立即播放测试音效
  ↓
按钮变为 "✓ 音频已启用"（绿色）
```

**说明**：
- 浏览器安全策略要求用户主动激活
- 只需激活一次（除非刷新页面）
- 再次点击可以关闭音频
- 是**开关按钮**，可以切换开/关

---

### 2. 音量控制
**位置**：闹铃面板右上角，音频按钮旁边

**操作**：
```
点击 🔊 图标 → 切换音效开/关
点击 百分比 (如 "50%") → 显示音量滑块
拖动滑块 → 调整 0-100%
```

**说明**：
- 音量为 0 时等同于静音
- 关闭🔊后即使激活音频也不播放
- 音量设置会自动保存

---

## 🖥️ 二、桌面通知（Mac特殊配置）

### 第一步：浏览器内授权
**位置**：闹铃面板右上角

**操作**：
```
点击 "🖥️ 启用通知" 按钮
  ↓
浏览器弹出权限请求
  ↓
点击 "允许"
```

---

### 第二步：Mac系统配置（如果第一步没反应）

#### 方法1：系统设置
```bash
1. 打开 Launchpad 或 Spotlight（⌘ + 空格）
2. 搜索并打开 "系统设置" (System Settings)
3. 点击 "通知" (Notifications)
4. 在左侧列表中找到你的浏览器：
   • Google Chrome
   • Safari
   • Arc
   • Brave
   • 等其他浏览器
5. 点击浏览器名称
6. 开启以下选项：
   ✅ 允许通知
   ✅ 横幅
   ✅ 声音
   ✅ 在通知中心显示
```

#### 方法2：快捷配置
```
按住 Option 键，点击菜单栏右上角的通知中心图标
  ↓
打开系统设置 → 通知
  ↓
找到浏览器 → 开启通知
```

---

### 测试桌面通知

**步骤**：
1. 完成上述配置
2. 刷新 Trade View 页面
3. 等待价格提醒触发（黄色闹铃面板出现）
4. 观察屏幕**右上角**

**预期效果**：
```
┌──────────────────────────────┐
│ 🔔 Trade View 价格提醒        │
│                               │
│ 600879 - 某某股票             │
│ 🎉 止盈提醒                   │
│ 当前价格: ¥31.56              │
└──────────────────────────────┘
```

**没看到通知？检查：**
- [ ] 系统设置 → 通知 → 浏览器 → 已开启
- [ ] 系统设置 → 勿扰模式 → 已关闭
- [ ] 浏览器权限已允许
- [ ] 刷新页面后重新测试

---

## 📧 三、邮箱提醒

### 位置
**用户面板**（Dashboard 左侧第一个卡片） → **用户信息** → 往下滚动

### 外观识别
```
┌─────────────────────────────────┐
│ 📧 邮箱闹铃提醒                  │
│ 价格触及止损/止盈时发送邮件通知  │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ ○ 点击开启邮件提醒 (灰色)   │ │  ← 未开启
│ └─────────────────────────────┘ │
└─────────────────────────────────┘

点击后变为：

┌─────────────────────────────────┐
│ 📧 邮箱闹铃提醒                  │
│ 价格触及止损/止盈时发送邮件通知  │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ ✓ 邮件提醒已开启 (绿色)     │ │  ← 已开启
│ └─────────────────────────────┘ │
│                                 │
│ ✓ 邮件通知已启用，价格提醒将... │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ 📧 发送测试邮件             │ │  ← 测试按钮
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

---

### 配置邮箱服务（必需！）

#### 步骤1：创建后端配置文件

打开终端，执行：

```bash
cd /Users/ierx/cursor_workspace/trade-view/backend

cat > .env << 'EOF'
# SMTP 邮件服务配置
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=你的Gmail地址@gmail.com
SMTP_PASSWORD=你的应用专用密码
SENDER_EMAIL=你的Gmail地址@gmail.com
SENDER_NAME=Trade View 价格提醒
EOF
```

---

#### 步骤2：获取Gmail应用专用密码

**为什么需要？**
- Gmail不允许直接使用账户密码发送邮件
- 必须创建"应用专用密码"

**如何获取：**

1. 访问：https://myaccount.google.com/apppasswords

2. 登录你的Gmail账户

3. 点击"选择应用" → **邮件**

4. 点击"选择设备" → **Mac**

5. 点击"生成"

6. 复制16位密码（格式：xxxx xxxx xxxx xxxx）

7. 粘贴到`.env`文件的`SMTP_PASSWORD=`后面

**示例配置：**
```bash
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=zhangsan@gmail.com
SMTP_PASSWORD=abcd efgh ijkl mnop
SENDER_EMAIL=zhangsan@gmail.com
SENDER_NAME=Trade View 价格提醒
```

---

#### 步骤3：重启后端服务

```bash
cd /Users/ierx/cursor_workspace/trade-view/backend

# 停止当前运行的后端（Ctrl+C）
# 然后重新启动
python3 main.py
```

---

### 测试邮箱提醒

1. **开启邮件提醒**
   - 用户面板 → 📧 邮箱闹铃提醒
   - 点击大按钮（变绿）

2. **发送测试邮件**
   - 点击 "📧 发送测试邮件" 按钮

3. **检查邮箱**
   - 等待 30秒 - 1分钟
   - 检查 **收件箱**
   - 检查 **垃圾邮件** 文件夹
   - 检查 **促销** 标签页
   - 检查 **更新** 标签页

4. **预期邮件内容**
```
发件人：Trade View 价格提醒
主题：Trade View 测试邮件
内容：JOJO风格HTML邮件
      包含测试股票信息
```

---

### 没收到邮件？排查步骤

#### 1. 检查后端日志
```bash
cd backend
tail -n 50 trade_view.log | grep email
```

#### 2. 检查配置
```bash
cat .env | grep SMTP
```

#### 3. 测试SMTP连接
```bash
python3 -c "
import smtplib
from email.mime.text import MIMEText
msg = MIMEText('测试邮件')
msg['Subject'] = '测试'
msg['From'] = '你的Gmail'
msg['To'] = '你的Gmail'
with smtplib.SMTP('smtp.gmail.com', 587) as server:
    server.starttls()
    server.login('你的Gmail', '应用专用密码')
    server.send_message(msg)
print('✅ 发送成功！')
"
```

#### 4. 常见问题
- ❌ **密码错误**：确认使用应用专用密码，不是Gmail密码
- ❌ **连接被拒绝**：检查防火墙设置
- ❌ **Gmail安全设置**：访问 https://myaccount.google.com/security 检查"不够安全的应用的访问权限"

---

## 🎯 完整测试清单

### ✅ 音效系统
- [ ] 点击"🎵 激活音频"按钮
- [ ] 听到Giorno's Theme测试音效
- [ ] 按钮变为"✓ 音频已启用"
- [ ] 点击🔊切换音效开关
- [ ] 点击百分比调整音量
- [ ] 音量为0时无声音
- [ ] 再次点击"✓ 音频已启用"可关闭

### ✅ 桌面通知（Mac）
- [ ] 点击"🖥️ 启用通知"
- [ ] 浏览器弹出权限请求
- [ ] 点击"允许"
- [ ] 如没反应：系统设置 → 通知 → 浏览器 → 开启
- [ ] 刷新页面
- [ ] 等待价格提醒
- [ ] 屏幕右上角出现通知

### ✅ 邮箱提醒
- [ ] 打开用户面板
- [ ] 找到"📧 邮箱闹铃提醒"
- [ ] 创建`.env`配置文件
- [ ] 获取Gmail应用专用密码
- [ ] 填写SMTP配置
- [ ] 重启后端服务
- [ ] 点击大按钮开启（变绿）
- [ ] 点击"📧 发送测试邮件"
- [ ] 等待30秒-1分钟
- [ ] 检查邮箱（包括垃圾邮件）

---

## ⚠️ 注意事项

### AudioContext 警告
浏览器autoplay policy的限制，控制台可能仍会显示警告：
```
The AudioContext was not allowed to start. It must be resumed...
```

**这是正常的！**
- 浏览器级别警告，JavaScript无法完全阻止
- **不影响功能使用**
- 点击"激活音频"后音效就能正常播放
- 可以安全忽略这些警告

**如果想减少警告：**
1. Chrome: `chrome://settings/content/sound`
2. 勾选"允许网站播放声音"
3. 刷新页面

---

### Mac特殊权限
Mac对通知和音频有更严格的权限管理：
- **通知**：需要在系统设置中手动配置
- **音频**：需要用户主动点击激活
- **邮件**：需要Gmail应用专用密码

---

## 📞 问题反馈

如果按照本指南操作后仍有问题，请提供：
1. Mac系统版本（例如：macOS Sonoma 14.0）
2. 浏览器版本（例如：Chrome 120.0）
3. 控制台截图（F12 → Console）
4. 后端日志（backend/trade_view.log）

---

**最后更新：2026-01-12**
