# 🚀 启动和日志监控指南

## 📋 快速启动步骤

### 方式一：分别启动（推荐用于开发调试）

#### 1. 启动后端

**打开终端1：**
```bash
cd /Users/ierx/cursor_workspace/trade-view/backend
python3 main.py
```

或者后台运行并保存日志：
```bash
cd /Users/ierx/cursor_workspace/trade-view/backend
python3 main.py > backend.log 2>&1 &
```

**验证后端启动：**
```bash
curl http://localhost:3000/
# 应该返回: {"message":"A股交易管理系统 API","docs":"/docs"}
```

#### 2. 启动前端

**打开终端2：**
```bash
cd /Users/ierx/cursor_workspace/trade-view/frontend
npm install  # 如果还没安装依赖
npm run dev
```

前端会自动打开浏览器，或手动访问：http://localhost:5173

### 方式二：使用npm脚本（需要先安装依赖）

```bash
# 在项目根目录
npm run dev
```

这会同时启动前端和后端（如果配置了concurrently）。

## 📊 日志监控

### 后端日志监控

#### 方法1：实时查看日志文件（推荐）

**打开终端3（用于监控日志）：**
```bash
cd /Users/ierx/cursor_workspace/trade-view/backend
tail -f backend.log
```

#### 方法2：直接查看终端输出

如果后端是在前台运行的（`python3 main.py`），日志会直接显示在终端。

#### 方法3：查看最近的日志

```bash
# 查看最后50行日志
tail -50 backend/backend.log

# 查看最后100行并实时更新
tail -100f backend/backend.log

# 搜索错误日志
grep ERROR backend/backend.log

# 搜索特定API的日志
grep "/api/auth" backend/backend.log
```

### 前端日志监控

#### 方法1：浏览器开发者工具（推荐）

1. 打开浏览器访问 http://localhost:5173
2. 按 `F12` 或 `Cmd+Option+I` (Mac) 打开开发者工具
3. 切换到 **Console** 标签页
4. 所有前端日志都会显示在这里，带有emoji标识：
   - 🔐 认证相关
   - 💰 资金相关
   - 📡 API请求
   - ✅ 成功操作
   - ❌ 错误信息

#### 方法2：终端输出

前端启动后，Vite会在终端显示：
- 编译信息
- 热更新信息
- 错误信息

### 同时监控前后端日志

**打开3个终端窗口：**

**终端1 - 后端：**
```bash
cd /Users/ierx/cursor_workspace/trade-view/backend
python3 main.py
```

**终端2 - 前端：**
```bash
cd /Users/ierx/cursor_workspace/trade-view/frontend
npm run dev
```

**终端3 - 日志监控：**
```bash
cd /Users/ierx/cursor_workspace/trade-view/backend
tail -f backend.log
```

## 🔍 日志格式说明

### 后端日志格式

```
[时间戳] - [模块] - [级别] - [消息]
```

**示例：**
```
2026-01-11 22:14:26 - main - INFO - 🚀 启动 A股交易管理系统后端服务
2026-01-11 22:14:26 - auth - INFO - 🔐 [注册] 用户名: testuser, 邮箱: test@example.com
2026-01-11 22:14:26 - main - INFO - 📥 [2026-01-11 22:14:26] POST /api/auth/register
2026-01-11 22:14:26 - main - INFO - 📤 [2026-01-11 22:14:26] ✅ POST /api/auth/register - 201 (0.123s)
```

### 前端日志格式

```
[时间] [emoji] [级别] [模块] [消息]
数据: {...}
```

**示例：**
```
[22:14:26] 🔐 [INFO] [Auth] 初始化认证上下文
[22:14:26] ✅ [INFO] [Auth] 登录成功
数据: { userId: 1, username: "testuser" }
```

## 🛠️ 常用命令

### 检查服务状态

```bash
# 检查后端是否运行
curl http://localhost:3000/ || echo "后端未运行"

# 检查端口占用
lsof -i :3000  # 后端端口
lsof -i :5173  # 前端端口

# 查看后端进程
ps aux | grep "python.*main.py" | grep -v grep
```

### 停止服务

```bash
# 停止后端
pkill -f "python.*main.py"

# 或者找到进程ID后kill
ps aux | grep "python.*main.py"
kill <PID>

# 停止前端（Ctrl+C 在运行前端的终端）
```

### 重启服务

```bash
# 停止后端
pkill -f "python.*main.py"

# 重新启动后端
cd backend && python3 main.py > backend.log 2>&1 &
```

## 📝 日志过滤技巧

### 后端日志过滤

```bash
# 只看错误
tail -f backend.log | grep ERROR

# 只看认证相关
tail -f backend.log | grep -E "(注册|登录|auth)"

# 只看API请求
tail -f backend.log | grep -E "(📥|📤)"

# 排除SQL日志（减少噪音）
tail -f backend.log | grep -v "sqlalchemy.engine"
```

### 前端日志过滤

在浏览器Console中使用：
- 点击Console右上角的过滤器图标
- 选择日志级别（Errors, Warnings, Info等）
- 或使用搜索框搜索关键词

## 🐛 调试流程

### 1. 启动服务

```bash
# 终端1 - 启动后端
cd backend && python3 main.py

# 终端2 - 启动前端
cd frontend && npm run dev

# 终端3 - 监控后端日志
cd backend && tail -f backend.log
```

### 2. 打开浏览器

- 访问 http://localhost:5173
- 打开开发者工具 (F12)
- 切换到Console标签

### 3. 测试功能

1. **注册用户** → 观察前后端日志
2. **登录** → 观察token生成
3. **查看用户面板** → 观察API调用
4. **添加交易记录** → 观察数据库操作

### 4. 排查问题

- **后端错误**: 查看终端1或backend.log
- **前端错误**: 查看浏览器Console
- **API错误**: 查看浏览器Network标签 + 后端日志
- **数据库错误**: 查看后端日志中的SQL错误

## 💡 实用脚本

### 一键启动脚本（可选）

创建 `start.sh`:

```bash
#!/bin/bash

# 启动后端
cd backend
python3 main.py > backend.log 2>&1 &
BACKEND_PID=$!
echo "后端已启动，PID: $BACKEND_PID"

# 等待后端启动
sleep 3

# 启动前端
cd ../frontend
npm run dev &
FRONTEND_PID=$!
echo "前端已启动，PID: $FRONTEND_PID"

echo "服务已启动！"
echo "后端: http://localhost:3000"
echo "前端: http://localhost:5173"
echo "日志: tail -f backend/backend.log"
echo ""
echo "按 Ctrl+C 停止服务"

# 等待中断信号
trap "kill $BACKEND_PID $FRONTEND_PID; exit" INT
wait
```

使用：
```bash
chmod +x start.sh
./start.sh
```

## ✅ 验证清单

启动后检查：

- [ ] 后端响应: `curl http://localhost:3000/` 返回JSON
- [ ] Swagger UI: http://localhost:3000/docs 可以访问
- [ ] 前端页面: http://localhost:5173 可以访问
- [ ] 后端日志: 有启动成功的日志
- [ ] 前端日志: 浏览器Console没有错误

## 🎯 现在开始

1. **打开3个终端窗口**
2. **终端1**: 启动后端
3. **终端2**: 启动前端
4. **终端3**: 监控日志
5. **打开浏览器**: 访问 http://localhost:5173
6. **打开开发者工具**: 查看前端日志

开始你的调试之旅吧！🚀
