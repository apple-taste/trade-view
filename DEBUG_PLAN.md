# 🎯 前端调试计划 - 可视化指南

## 📅 调试会话概览

**日期**: $(date)  
**前端状态**: ✅ 运行中 (http://localhost:5173)  
**后端状态**: ✅ 运行中 (http://localhost:3000)  
**日志文件**: `frontend_dev.log`

---

## 🗺️ 调试流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    开始调试会话                                │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │  1. 检查服务状态      │
            │  - 前端: localhost:5173│
            │  - 后端: localhost:3000│
            └──────────┬───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │  2. 打开浏览器        │
            │  - 访问: localhost:5173│
            │  - 打开DevTools (F12) │
            └──────────┬───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │  3. 检查Console       │
            │  - 查看错误/警告      │
            │  - 检查日志输出       │
            └──────────┬───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │  4. 检查Network       │
            │  - API请求状态        │
            │  - 响应内容           │
            └──────────┬───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │  5. 实时监控日志      │
            │  - tail -f frontend_dev.log│
            │  - ./monitor_frontend.sh│
            └──────────┬───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │  6. 问题定位          │
            │  - 查看错误堆栈       │
            │  - 检查代码逻辑       │
            └──────────┬───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │  7. 修复并验证        │
            │  - 修改代码           │
            │  - 热重载测试         │
            └──────────────────────┘
```

---

## 🔍 调试检查清单

### Phase 1: 环境检查 ✅

- [x] 前端服务器运行中
- [x] 后端API运行中
- [x] 端口未被占用
- [x] 日志文件可访问

### Phase 2: 浏览器检查

- [ ] 页面正常加载
- [ ] Console无错误
- [ ] Network请求成功
- [ ] React组件正常渲染

### Phase 3: 功能测试

- [ ] 用户登录/注册
- [ ] 交易记录CRUD
- [ ] 持仓面板显示
- [ ] 价格提醒功能
- [ ] AI分析功能

---

## 🛠️ 调试工具使用指南

### 1. 实时日志监控

#### 方式A: 使用监控脚本（推荐）
```bash
./monitor_frontend.sh
```

**输出示例**:
```
🔍 前端调试监控面板
====================

📝 实时日志 (Ctrl+C 退出)
---
✅ VITE v5.4.21  ready in 107 ms
🌐 ➜  Local:   http://localhost:5173/
❌ [plugin:vite:css] Error: ...
⚠️  [vite] warning: ...
```

#### 方式B: 直接查看日志
```bash
# 实时查看
tail -f frontend_dev.log

# 查看最后50行
tail -n 50 frontend_dev.log

# 搜索错误
grep -i error frontend_dev.log
```

### 2. 快速调试工具

```bash
./quick_debug.sh
```

**菜单选项**:
```
🚀 快速调试工具
================

选择操作:
1) 查看前端日志
2) 查看后端日志
3) 检查端口占用
4) 检查进程状态
5) 重启前端
6) 实时监控前端日志
```

### 3. 浏览器DevTools

#### Console面板快捷键
```
Cmd+K (Mac) / Ctrl+L (Windows): 清空控制台
Cmd+Shift+P: 打开命令面板
Cmd+Option+J: 直接打开Console
```

#### 常用调试命令
```javascript
// 检查Token
localStorage.getItem('token')

// 检查用户信息
localStorage.getItem('user')

// 清除所有数据
localStorage.clear()

// 检查API连接
fetch('/api/user/profile')
  .then(r => r.json())
  .then(console.log)
```

---

## 📊 问题诊断矩阵

| 症状 | 可能原因 | 检查方法 | 解决方案 |
|------|---------|---------|---------|
| 页面白屏 | 1. JS错误<br>2. 路由问题<br>3. API失败 | Console面板<br>Network面板 | 查看错误堆栈<br>检查路由配置 |
| API 401 | Token过期 | Network → 查看请求头 | 重新登录 |
| API 404 | 路径错误 | Network → 查看URL | 检查API路径 |
| 样式不显示 | Tailwind未编译 | Elements → Styles | 检查tailwind.config.js |
| 组件不更新 | State未更新 | React DevTools | 检查Context/State |
| 热重载失败 | 文件监听问题 | 终端日志 | 重启开发服务器 |

---

## 🎨 可视化调试面板

### 终端监控面板布局

```
┌─────────────────────────────────────────────────┐
│  🔍 前端调试监控面板                              │
│  ==============================================  │
│                                                  │
│  📝 实时日志 (Ctrl+C 退出)                       │
│  ────────────────────────────────────────────   │
│                                                  │
│  ✅ VITE v5.4.21  ready in 107 ms               │
│  🌐 ➜  Local:   http://localhost:5173/         │
│                                                  │
│  ✅ [vite] connecting...                        │
│  ✅ [vite] connected.                          │
│                                                  │
│  ⚠️  [vite] warning: ...                        │
│  ❌ [plugin:vite:css] Error: ...                │
│                                                  │
└─────────────────────────────────────────────────┘
```

### 浏览器DevTools布局

```
┌─────────────────────────────────────────────────┐
│  Chrome DevTools                                 │
├──────────┬──────────────────────────────────────┤
│ Elements │  <div className="jojo-card">         │
│          │    <h2>交易记录</h2>                  │
│          │  </div>                               │
├──────────┼──────────────────────────────────────┤
│ Console  │  🔍 调试信息: {...}                   │
│          │  ✅ API请求成功                        │
│          │  ❌ Error: ...                         │
├──────────┼──────────────────────────────────────┤
│ Network  │  GET /api/trades 200 OK              │
│          │  POST /api/trades 201 Created        │
│          │  GET /api/user/profile 401 Unauthorized│
├──────────┼──────────────────────────────────────┤
│ Sources  │  TradeHistoryPanel.tsx               │
│          │  ┌─────────────────────────────┐    │
│          │  │ 125│ handleDelete() {        │    │
│          │  │ 126│   debugger; ← 断点      │    │
│          │  │ 127│   ...                   │    │
│          │  └─────────────────────────────┘    │
└──────────┴──────────────────────────────────────┘
```

---

## 🚀 快速调试命令参考

### 启动服务
```bash
# 前端
cd frontend && npm run dev

# 后端（另一个终端）
cd backend && python main.py
```

### 监控日志
```bash
# 前端日志
tail -f frontend_dev.log

# 后端日志
tail -f backend/backend.log

# 同时监控
tail -f frontend_dev.log backend/backend.log
```

### 检查状态
```bash
# 检查端口
lsof -i :5173  # 前端
lsof -i :3000  # 后端

# 检查进程
ps aux | grep vite
ps aux | grep uvicorn

# 检查API
curl http://localhost:3000/api/health
```

### 清理和重启
```bash
# 清理前端
pkill -f vite
rm -rf frontend/node_modules/.vite
cd frontend && npm run dev

# 清理后端
pkill -f uvicorn
cd backend && python main.py
```

---

## 📈 性能监控

### 1. 页面加载时间
```
浏览器 → Performance → Record → 刷新页面 → Stop
查看:
- FCP (First Contentful Paint)
- LCP (Largest Contentful Paint)
- TTI (Time to Interactive)
```

### 2. API响应时间
```
浏览器 → Network → 选择API请求 → Timing
查看:
- Waiting (TTFB)
- Content Download
- 总耗时
```

### 3. React渲染性能
```
React DevTools → Profiler → Record → 操作 → Stop
查看:
- 组件渲染时间
- 渲染次数
- 性能瓶颈
```

---

## 🎯 常见调试场景

### 场景1: 页面加载失败
```
1. 检查Console错误
2. 检查Network请求
3. 检查终端日志
4. 验证API连接
```

### 场景2: 数据不显示
```
1. 检查API响应（Network）
2. 检查组件State（React DevTools）
3. 检查数据格式
4. 检查过滤条件
```

### 场景3: 样式问题
```
1. 检查Elements → Styles
2. 检查Tailwind类名
3. 检查CSS文件加载
4. 检查响应式断点
```

### 场景4: 交互不响应
```
1. 检查事件绑定
2. 检查State更新
3. 检查错误处理
4. 使用断点调试
```

---

## 📝 调试日志模板

### 错误报告模板
```
【错误类型】: [Error/Warning/Info]
【发生时间】: YYYY-MM-DD HH:MM:SS
【错误信息】: 
【错误堆栈】:
【相关文件】:
【复现步骤】:
【预期行为】:
【实际行为】:
```

### 调试记录模板
```
【调试会话】: YYYY-MM-DD
【问题描述】:
【检查步骤】:
  1. 
  2. 
  3. 
【解决方案】:
【验证结果】:
```

---

## 🔗 相关资源

- **完整调试指南**: `FRONTEND_DEBUG_GUIDE.md`
- **快速调试脚本**: `./quick_debug.sh`
- **监控脚本**: `./monitor_frontend.sh`
- **前端日志**: `frontend_dev.log`
- **后端日志**: `backend/backend.log`

---

**🎉 开始调试吧！使用 `./monitor_frontend.sh` 实时监控日志！**
